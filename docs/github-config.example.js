/**
 * Copy this file to github-config.local.js and set your token.
 * github-config.local.js is gitignored — never commit tokens.
 *
 * Revoke any token that was ever pasted into chat or committed by mistake.
 */
window.__GITHUB_FAVORITES_TOKEN__ = "";
